[mysqld]
user                    = mysql
pid-file                = /run/mysqld/mysqld.pid
datadir                 = /var/lib/mysql
tmpdir                  = /tmp
bind-address            = 0.0.0.0
character-set-server  = utf8mb4
